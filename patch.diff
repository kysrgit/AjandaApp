--- style.css
+++ style.css
@@ -29,6 +29,11 @@
   --shadow-glow: 0 0 40px rgba(59, 130, 246, 0.15);
 }

+:where(button, a, input, select, textarea):focus-visible {
+  outline: 2px solid var(--fitness-color);
+  outline-offset: 2px;
+}
+
 /* ---------- Reset & Base ---------- */
 *,
 *::before,
